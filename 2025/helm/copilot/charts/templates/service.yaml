apiVersion: v1
kind: Service
metadata:
  name: {{ include "copilot-service.fullname" . }}
  labels:
    {{- include "copilot-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "copilot-service.selectorLabels" . | nindent 4 }}